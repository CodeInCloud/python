import sys
sys.getrefcount(1)
>>> 599
X = 1
sys.getrefcount(1)
>>> 600

ps: python 中数字和常量不能被改变， 故对同一对象无论有多少个引用都没有关系的. 
python 缓存并复用了小的正数和字符串。 这里的对象并不像我们所说的那样被回收；
相反的， 它将可能仍然被保存在一个系统表中，等待下一次你的代码生成另一个 1 来复用。
对于那些不被回收的没缓存机制与代码并没有什么关系。


